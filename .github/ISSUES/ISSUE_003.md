# Issue #3: RSI calculation accuracy issues in fallback mode

**Description:**
The manual RSI calculation in `tools/technical_analysis.py` seems to differ significantly from standard implementations when `pandas_ta` is not present.

**Labels:** bug, enhancement
